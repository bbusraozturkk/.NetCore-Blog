@model EntityLayer.Concrete.Writer
@{
    ViewData["Title"] = "WriterEditProfile";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}

<h1>WriterEditProfile</h1>
@using (Html.BeginForm("WriterEditProfile", "Writer", FormMethod.Post))
{
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Yazar Profil Bilgileri Düzenleme</h4>
                <p class="card-description"> Basic form elements </p>
                @Html.HiddenFor(x => x.WriterID, new { @class = "form-control" })
                <form class="forms-sample">
                    <div class="form-group">
                        <label for="exampleInputName1">Name</label>
                        @Html.TextBoxFor(x => x.WriterName, new { @class = "form-control" })

                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail3">Email address</label>
                        @Html.TextBoxFor(x => x.WriterMail, new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword4">Password</label>
                        @Html.PasswordFor(x => x.WriterPassword, new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        <label>Yazar Görsel</label>
                        <input type="file" name="img[]" class="file-upload-default">
                        <div class="input-group col-xs-12">
                            <input type="text" class="form-control file-upload-info" disabled placeholder="Upload Image">
                            <span class="input-group-append">
                                <button class="file-upload-browse btn btn-gradient-primary" type="button">Upload</button>
                            </span>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="exampleTextarea1">Yazar Hakkında</label>
                        @Html.TextAreaFor(x => x.WriterAbout, new { @class = "form-control" })
                    </div>
                    <button type="submit" class="btn btn-gradient-primary mr-2">Submit</button>
                    <button class="btn btn-light">Cancel</button>
                </form>
            </div>
        </div>
    </div>
}







@* 

@using CoreDemo.Controllers
@model List<Class1>

<table>
    <tr>
        <th>#</th>
        <th>Adı soyadı</th> 
    </tr>
    @foreach(var item in Model)
    {
        <tr>
            <td>@item.ID</td>
            <td>@item.Name</td>
            <td><a href="/EmployeeTest/DeleteEmployee/@item.ID" class="btn-primary btn-outline-danger">sil</a></td>
            <td><a href="/EmployeeTest/EditEmployee/@item.ID" class="btn-primary btn-outline-success">güncelle</a></td>
        </tr>
    }
</table>
<td><a href="/EmployeeTest/AddEmployee/" class="btn-primary btn-outline-success">Yeni Çalışan Girirşi</a></td> *@






@* @using CoreDemo.Controllers
@model Class1

<form method="post">
    <label>Çalışan Adı Soyadı</label>
    <input type="text" asp-for="@Model.Name" />
    <button type="submit" class="btn btn-info">Çalışan Ekle</button>
</form> *@



@* 
@model CoreDemo.Controllers.Class1

<form method="post">
    <label>ID</label>
    @Html.TextBoxFor(x=>x.ID)
    <label>Personel</label>
    @Html.TextBoxFor(x => x.Name)
    <button class="btn btn-success">Güncelle</button>
</form> *@



@* 
@model UserSignUpViewModel

<body>
    <form>
        123. video 23:28
    </form>
</body> *@